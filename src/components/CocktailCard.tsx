import { Link } from 'react-router-dom'
import 

type CocktailCardProps = {
	image: string
	name: string
	id: string
	glass: string
}

const CocktailCard = ({ image, name, id, glass }: CocktailCardProps) => {
	return <div>Card</div>
}

export default CocktailCard
